/**
 * Public entrypoint — exports the SDK client factory and CLI internals
 * for embedding / testing. The actual binary entry is src/cli.ts.
 */

export { NAME, REPO, VERSION } from "./version.js";
